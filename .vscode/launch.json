{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug TPMiddle",
            "program": "${workspaceFolder}/bin/tpmiddle",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
            "sourceLanguages": [
                "swift",
                "cpp"
            ],
            "env": {
                "DYLD_FRAMEWORK_PATH": "${workspaceFolder}/build",
                "DYLD_LIBRARY_PATH": "${workspaceFolder}/build"
            }
        }
    ]
}
